<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Documents directory for certificates -->
    <external-path 
        name="documents" 
        path="Documents/SecureWipe" />
    
    <!-- App's private external files directory -->
    <external-files-path 
        name="external_files" 
        path="." />
    
    <!-- App's cache directory -->
    <cache-path 
        name="cache" 
        path="." />
</paths>